const Matchers = DSL.matchers<Option.HKT>()

/**
 * @tsplus static Option/Ops match
 */
export const match = Matchers.match

/**
 * @tsplus static Option/Ops matchTag
 */
export const matchTag = Matchers.matchTag

/**
 * @tsplus static Option/Ops matchTagIn
 */
export const matchTagIn = Matchers.matchTagIn

/**
 * @tsplus static Option/Ops matchMorph
 */
export const matchMorph = Matchers.matchMorph

/**
 * @tsplus static Option/Ops matchIn
 */
export const matchIn = Matchers.matchIn
